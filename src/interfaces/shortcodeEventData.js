export class shortcodeEventData {
  /**
   * @param {string} title - The title of the timer.
   * @param {string} issueId - The issue ID of the timer.
   */
  constructor(title, issueId) {
    this.title = title;
    this.issueId = issueId;
  }
}
